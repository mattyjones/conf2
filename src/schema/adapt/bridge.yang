module bridge {
    prefix "bridge";
    namespace "conf2.org/bridge";
    revision 0000-00-00;
    grouping map {
        container map {
            list mappings {
                key "from";
                leaf-list left {
                    type string;
                }
                leaf-list right {
                    type string;
                }
            }
            uses map;
        }
    }
    uses map;
}
